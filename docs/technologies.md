# Reco stack — module web-scraping *(pointeur)*

> Ce fichier a été **fusionné** : la reco de stack du module vit désormais ailleurs. Ne rien ajouter ici.

La **stack technologique** du module (décisions, détail explicatif **par brique**, cascade d'escalade anti-bot
et lacunes) est maintenue dans **[`08-stack-techno.md`](08-stack-techno.md)** :

- §2 — aperçu par domaine **+ détail par brique** (rôle, réserve, alternatives) ;
- §3 — analyser pour naviguer ≠ extraire le sens ;
- §4 — **cascade d'escalade** (table de repli, ordre à coût croissant, entrée adaptative) ;
- §5 — **lacunes** (briques comblées vs à coder vs différées pré-prod) ;
- §6 — points ouverts **+ décisions actées**.

Les **états** des technos (Sélectionné / À suivre / En attente / Refusé) et la colonne **« Rang / repli »**
vivent dans le référentiel du module **`technologies.xlsx`** (feuille `web-scraping`). La « feuille comparatifs »
citée par les ADR appartient au **référentiel monorepo** `technologies-referentiel.xlsx`, pas à ce module.
